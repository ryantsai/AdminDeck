# settings.dashboardPerformance

- **English value**: `Performance`
- **Namespace**: `settings`
- **File/component**: `src/settings/DashboardSettings.tsx`
- **UI role**: `label` (fieldset legend)
- **User flow**: Settings → Dashboard → second group below "General". Houses the active-widget cap input and any future Dashboard performance knobs. Sits inside the standard `settings-subsection settings-fieldset` treatment.
- **Tone**: short, neutral; matches sibling labels like "General".
- **Placeholders**: none.
- **Domain notes**: Keep it short and category-style — this is a fieldset legend, not a sentence. If "Performance" is awkward in the target language, "Resources" or "Rendering" are acceptable alternatives.

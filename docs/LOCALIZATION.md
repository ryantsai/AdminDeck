# Localization Backlog

This file tracks English source strings that still need translation. Product implementation is English first: add or update `src/i18n/locales/en.json` during feature work, then document any untranslated keys here with enough context for later localization.

When a key is translated into every supported locale, remove its entry from this file.

## Pending Strings

- App Launcher widget keys added for the Dashboard App Launcher flow
  - Keys: `appLauncher.title`, `appLauncher.moduleLabel`, `appLauncher.subtitle`, `appLauncher.statusReady`, `appLauncher.summaryLabel`, `appLauncher.pinnedApps`, `appLauncher.railShortcuts`, `appLauncher.railShortcutsLabel`, `appLauncher.entriesLabel`, `appLauncher.addApp`, `appLauncher.selectFileTitle`, `appLauncher.fileFilter`, `appLauncher.loading`, `appLauncher.emptyTitle`, `appLauncher.emptyHint`, `appLauncher.dialogLabel`, `appLauncher.dialogTitle`, `appLauncher.name`, `appLauncher.path`, `appLauncher.arguments`, `appLauncher.argumentsPlaceholder`, `appLauncher.workingDirectory`, `appLauncher.workingDirectoryPlaceholder`, `appLauncher.pinToRail`, `appLauncher.railPinned`, `appLauncher.missingPath`, `appLauncher.launchApp`, `appLauncher.editApp`, `appLauncher.removeApp`, `appLauncher.moreActions`, `appLauncher.runNormal`, `appLauncher.runAdmin`, `appLauncher.runAsUser`, `appLauncher.edit`, `appLauncher.remove`, `appLauncher.loadError`, `appLauncher.saveError`, `appLauncher.launchError`, `appLauncher.savedStatus`, `appLauncher.removedStatus`, `appLauncher.launchStatus`
  - English values: See `src/i18n/locales/en.json` under the `appLauncher` namespace.
  - Namespace: `appLauncher`
  - File/component: current implementation uses `src/app-launcher/AppLauncherPage.tsx`, `src/app/ActivityRail.tsx`, and `src/workspace/StatusBar.tsx`; target design should move this copy into Dashboard-owned widget components such as `src/dashboard/widgets.ts` or a dedicated Dashboard App Launcher widget component.
  - UI role: Dashboard widget title/subtitle, widget entry labels, buttons, dialog labels/placeholders, context-menu commands, warnings, and status-bar messages.
  - Flow: User opens Dashboard from the Activity Rail, adds an App Launcher widget to a Dashboard view, adds or edits a local app/shortcut/script/file entry inside that widget, launches it normally by activating its icon-and-text tile, or uses the right-click context menu for edit/remove/administrator/alternate-user launch when the entry is runnable.
  - Tone: concise, professional desktop utility UI; warnings and errors should be direct.
  - Placeholders: `{{name}}` is the launcher entry display name; `{{message}}` is a backend or runtime error string.
  - Domain notes: App Launcher entries are Dashboard widget data, not a standalone module and not Connections or Sessions. The default widget surface should show only an icon and text for each entry; management controls belong in an app-owned right-click menu. Administrator and alternate-user launch use native Windows prompts; KKTerm stores no launch credentials.

- RDP/VNC customizable option keys added for `src/settings/RdpSettings.tsx`, `src/settings/VncSettings.tsx`, and `src/connections/ConnectionSidebar.tsx`
  - Keys: `settings.rdpColorDepth32`, `settings.rdpColorDepth24`, `settings.rdpColorDepth16`, `settings.rdpColorDepth15`, `settings.rdpPerformanceBalanced`, `settings.rdpPerformanceQuality`, `settings.rdpPerformanceSpeed`, `settings.rdpRedirectClipboard`, `settings.rdpRedirectClipboardHint`, `settings.rdpRedirectDrives`, `settings.rdpRedirectDrivesHint`, `settings.rdpSettingsSaved`, `settings.vncEncodingTight`, `settings.vncEncodingZrle`, `settings.vncEncodingRaw`, `settings.vncColorFull`, `settings.vncColor256`, `settings.vncColor64`, `settings.vncColor8`, `settings.vncSharedSession`, `settings.vncSharedSessionHint`, `settings.vncViewOnly`, `settings.vncViewOnlyHint`, `settings.vncSettingsSaved`, `connections.rdpOptions`, `connections.vncOptions`, `connections.inheritSettingsDefaults`
  - English values: See `src/i18n/locales/en.json` under the `settings` and `connections` namespaces.
  - Namespace: `settings`, `connections`
  - File/component: `src/settings/RdpSettings.tsx`, `src/settings/VncSettings.tsx`, `src/connections/ConnectionSidebar.tsx`
  - UI role: Settings labels, select options, toggle labels/help text, save status, and per-Connection option group labels.
  - Flow: User sets global RDP/VNC defaults in Settings, then optionally overrides those defaults on an RDP or VNC Connection.
  - Tone: concise desktop administration UI; emphasize defaults vs per-Connection overrides.
  - Placeholders: none.
  - Domain notes: RDP, VNC, ActiveX, Tight, ZRLE, and Raw are technical protocol terms and usually remain English.

- Dashboard module keys added for `src/dashboard/DashboardPage.tsx`
  - Keys: `dashboard.title`, `dashboard.moduleLabel`, `dashboard.subtitle`, `dashboard.statusReady`, `dashboard.viewsLabel`, `dashboard.defaultView`, `dashboard.addView`, `dashboard.newViewName`, `dashboard.assistantContextLabel`, `dashboard.assistantContextSource`, `dashboard.assistantContextIntro`, `dashboard.catalog`, `dashboard.widgetCount`, `dashboard.categoriesLabel`, `dashboard.categories.hash`, `dashboard.categories.network`, `dashboard.categories.quick`, `dashboard.categories.report`, `dashboard.categoryAll`, `dashboard.playground`, `dashboard.playgroundHint`, `dashboard.addAgentWidget`, `dashboard.addWidget`, `dashboard.widgetAlreadySelected`, `dashboard.removeWidget`, `dashboard.deleteCustomWidget`, `dashboard.widgetDeleted`, `dashboard.emptyTitle`, `dashboard.emptyHint`, `dashboard.hashTitle`, `dashboard.hashSummary`, `dashboard.hashSample`, `dashboard.hashInput`, `dashboard.hashUnavailable`, `dashboard.characters`, `dashboard.bytes`, `dashboard.sha1`, `dashboard.sha256`, `dashboard.subnetTitle`, `dashboard.subnetSummary`, `dashboard.subnetSample`, `dashboard.subnetInput`, `dashboard.networkAddress`, `dashboard.broadcastAddress`, `dashboard.firstUsable`, `dashboard.lastUsable`, `dashboard.subnetMask`, `dashboard.wildcardMask`, `dashboard.totalAddresses`, `dashboard.usableHosts`, `dashboard.subnetError.invalidFormat`, `dashboard.subnetError.invalidAddress`, `dashboard.subnetError.invalidPrefix`, `dashboard.quickToolsTitle`, `dashboard.quickToolsSummary`, `dashboard.quickTool`, `dashboard.quickSample`, `dashboard.quickInput`, `dashboard.quickOutput`, `dashboard.quickToolOptions.urlEncode`, `dashboard.quickToolOptions.urlDecode`, `dashboard.quickToolOptions.base64Encode`, `dashboard.quickToolOptions.base64Decode`, `dashboard.quickToolOptions.unixToIso`, `dashboard.quickToolErrors.invalidInput`, `dashboard.quickToolErrors.invalidNumber`, `dashboard.reportTitle`, `dashboard.reportSummary`, `dashboard.reportBody`, `dashboard.copyValue`, `dashboard.agentWidgetDialogTitle`, `dashboard.agentWidgetDialogHint`, `dashboard.agentWidgetJson`, `dashboard.agentWidgetExample`, `dashboard.saveWidget`, `dashboard.agentWidgetSaved`, `dashboard.agentWidgetBuiltInId`, `dashboard.agentWidgetErrors.invalidJson`, `dashboard.agentWidgetErrors.invalidTitle`, `dashboard.agentWidgetErrors.invalidCategory`, `dashboard.agentWidgetErrors.invalidSummary`, `dashboard.agentWidgetErrors.invalidBody`
  - English values: See `src/i18n/locales/en.json` under the `dashboard` namespace.
  - Namespace: `dashboard`
  - File/component: `src/dashboard/DashboardPage.tsx`, `src/dashboard/widgets.ts`, `src/app/ActivityRail.tsx`, `src/workspace/StatusBar.tsx`
  - UI role: Activity Rail label, page title/subtitle, view tab labels, catalog labels, buttons, status messages, assistant page-context fragments, form labels, calculator output labels, validation errors, and report body text.
  - Flow: User opens Dashboard from the Activity Rail, switches or adds Dashboard views, selects prebuilt widgets per view, uses hash/subnet/quick-tool/App Launcher widgets, removes widgets, or pastes reviewed AI Assistant/coding-agent JSON to create a local report widget. The AI Assistant receives the active Dashboard view summary as page context while the Dashboard module is active.
  - Tone: concise, professional desktop utility UI; status/error copy should be direct and non-chatty.
  - Placeholders: `{{count}}` is the number of available widgets or the generated Dashboard view number; `{{name}}` is a widget title; `{{label}}` is the copied output label; `{{view}}` is the active Dashboard view title.
  - Domain notes: Dashboard is a widget playground, not the default launch state or a landing page. App Launcher belongs to Dashboard as a widget, not a peer Activity Rail module. Custom widgets are saved local UI definitions only; they do not execute generated code.
